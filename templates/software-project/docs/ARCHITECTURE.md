# Architecture

Describe the codebase at a level useful for coding agents.

## System Overview

`<one-paragraph overview>`

## Important Paths

| Path | Purpose |
|---|---|
| `<path>` | `<purpose>` |

## Data Flow

```text
<input> -> <component> -> <output>
```

## Extension Points

| Extension Point | Contract | Notes |
|---|---|---|
| `<name>` | `<interface or expectation>` | `<notes>` |

## Invariants

- `<invariant that must remain true>`
- `<invariant that tests should protect>`
